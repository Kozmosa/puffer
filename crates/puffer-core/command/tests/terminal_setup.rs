// Terminal setup tests live in `command_helpers::terminal_setup` so they can
// exercise explicit paths and terminal variants without mutating global
// process environment variables such as HOME, TERM_PROGRAM, or TMUX.
