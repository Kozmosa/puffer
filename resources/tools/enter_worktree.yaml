id: EnterWorktree
name: EnterWorktree
description: Create an isolated git worktree and switch into it.
handler: runtime:workflow:enter_worktree
approval_policy: auto
sandbox_policy: workspace-write
input_schema:
  type: object
  properties:
    name:
      type: string
      description: Optional name for the worktree.
